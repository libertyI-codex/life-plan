# 家計ライフプラン表 ローカル版 Ver.1.2 試作5

- ドル建生命保険の解約返戻金推移機能を追加しました。
- 米ドル建ての解約返戻金を円換算できます。
- 想定為替レートを入力できます。
- キャッシュバリュー表をCSV風テキストで入力できます。
- ライフプラン表に参考列として表示します。
- 今回の試作では総資産・純資産には自動反映していません。
- 二重計上防止のため、自動反映は次回以降に検討します。
- 保存キー `lifePlanLocal.data.v1` は変更していません。
- UI状態保存キー `lifePlanLocal.uiState.v1` を継続使用しています。

## ドル建生命保険の扱い

- ドル建生命保険の解約返戻金は総資産には含めない方針です。
- 純資産・年末資産見込みにも含めません。
- アプリ上では総資産外の参考資産として扱います。
- 二重計上防止、為替変動、流動性、税金未計算の観点からこの扱いにしています。
- 将来的に総資産へ自動反映する予定は現時点ではありません。

## Ver.1.2 試作3

- 計算開始年を変更できるようにしました。
- 計算終了年を変更できるようにしました。
- 現在資産の基準年を設定できるようにしました。
- 毎年、基準年と現在資産・収入・支出を更新して使い続けられる設計にしました。
- ライフプラン表の列グループを表示・非表示できるようにしました。
- ライフプラン表のコンパクト表示に対応しました。
- 列表示設定とコンパクト表示は `lifePlanLocal.uiState.v1` に保存します。
- 家計データ保存キー `lifePlanLocal.data.v1` は変更していません。
- ドル建生命保険は引き続き総資産外の参考資産として扱います。

## Ver.1.2 試作4

- ドル建生命保険列もコンパクト表示に対応しました。
- 表示列設定をライフプラン表のすぐ上に移動しました。
- イベント設定の「上へ」「下へ」「編集」「削除」ボタンを小型化しました。
- 今回は表示調整のみで、計算ロジックは変更していません。
- 家計データ保存キー `lifePlanLocal.data.v1` は変更していません。
- UI状態保存キー `lifePlanLocal.uiState.v1` は変更していません。

## GitHub Pages公開版 Ver.1.2 試作5

- GitHub Pages公開版を Ver.1.2 試作5相当に更新しました。
- コンパクト表示時、ドル建生命保険2列の列見出しを `返戻USD` / `返戻円` に短縮しました。
- USD列はコンパクト表示時に `USD` 表記を外し、数値のみで表示します。
- 円換算列は短縮円表示を使用します。
- ドル建生命保険2列専用CSSで幅・文字サイズ・paddingを調整しました。
- 表示のみの修正で、計算ロジックは変更していません。
- ドル建生命保険は引き続き総資産外の参考資産です。
- 家計データ保存キーは `lifePlanLocal.data.v1` のまま変更していません。
- UI状態保存キーは `lifePlanLocal.uiState.v1` を継続使用しています。
- GitHub Pages更新前にはJSONバックアップを取ってください。

## Ver.1.2 試作4 追加修正

- コンパクト表示時にドル建生命保険2列の列見出しも短縮するよう修正しました。
- コンパクト表示時の列見出しは「返戻USD」「返戻円」です。
- ドル建保険列の列幅・余白・文字サイズをさらに調整しました。
- 表示のみの修正で、計算ロジックは変更していません。
- 家計データ保存キー `lifePlanLocal.data.v1` は変更していません。
- UI状態保存キー `lifePlanLocal.uiState.v1` は変更していません。

## Ver.1.2 試作5

- ライフプラン表のドル建生命保険2列について、コンパクト表示時に本当に細く表示できるよう追加修正しました。
- コンパクト表示時に、ドル建生命保険2列の列見出しを短縮しました。
- 通常表示では従来どおりの列見出しを表示します。
- コンパクト表示時の列見出しは「返戻USD」「返戻円」です。
- USD列はコンパクト表示時に `USD` 表記を外し、数値のみで表示します。
- 円換算列は既存の短縮円表示を使用します。
- ドル建生命保険2列専用のCSSクラスを追加しました。
- コンパクト表示時だけ列幅を狭く調整しました。
- ヘッダー文字サイズを縮小しました。
- セル文字サイズを縮小しました。
- paddingをさらに縮小しました。
- `white-space: nowrap` で細い列のまま崩れにくく調整しました。
- 今回は表示のみの修正で、計算ロジックは変更していません。
- ドル建生命保険は引き続き、総資産外の参考資産です。
- ドル建生命保険の解約返戻金は、総資産・純資産・年末資産見込みには含めていません。
- 家計データ保存キー `lifePlanLocal.data.v1` は変更していません。
- UI状態保存キー `lifePlanLocal.uiState.v1` は変更していません。
